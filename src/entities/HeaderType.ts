import { IconType } from "./IconType";

export type HeaderType = {
  sourceLabel: string;
  sourceIcon?: IconType;
  purposeLabel: string;
  purposeIcon?: IconType;
  processLabel?: string;
  processIcon?: IconType;
}